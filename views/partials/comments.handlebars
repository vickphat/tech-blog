<div>
  {{#each this}}
    <div class="card">
        <h3 class="card-header"></h3>
        <section class="card-body">
            <div>
                {{comment_text}}
            </div>
            <div>
                -{{user.username}}, {{format_date created_at}}
            </div>
        </section>
    </div>
  {{/each}}
</div>
